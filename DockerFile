# Utilise une image de base avec Apache et PHP
FROM php:8.2-apache

# Installe les extensions PHP couramment utilisées
RUN docker-php-ext-install pdo pdo_mysql mbstring

# Active les modules Apache nécessaires
RUN a2enmod rewrite

# Copie les fichiers de configuration personnalisés (si nécessaire)
# COPY ./apache-config.conf /etc/apache2/sites-available/000-default.conf

# Copie le contenu du site web dans le répertoire par défaut d'Apache
COPY ./src/ /var/www/html/

# Expose le port 80 pour Apache
EXPOSE 80

# Démarre Apache au lancement du conteneur
CMD ["apache2-foreground"]